version: 2.1

# Optimized CircleCI pipeline for Dockerized Go Portfolio
executors:
  docker-executor:
    docker:
      - image: cimg/base:current
    resource_class: medium
    environment:
      DOCKER_BUILDKIT: 1

jobs:
  # Build and test the Docker image
  build-and-test:
    executor: docker-executor
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      
      - run:
          name: Build Docker image
          command: |
            echo "🐳 Building Go portfolio Docker image..."
            docker build -t portfolio-app:${CIRCLE_SHA1} .
            docker tag portfolio-app:${CIRCLE_SHA1} portfolio-app:latest
            
            # Show image info
            docker images portfolio-app
            echo "✅ Docker image built successfully"
      
      - run:
          name: Test Docker image
          command: |
            echo "🧪 Testing Docker image..."
            
            # Start container in background
            docker run -d --name test-portfolio -p 8000:8000 portfolio-app:latest
            
            # Wait for container to be ready
            echo "⏳ Waiting for container to be ready..."
            i=1
            while [ $i -le 30 ]; do
              if docker exec test-portfolio wget --no-verbose --tries=1 -O- http://localhost:8000/health > /dev/null 2>&1; then
                echo "✅ Container is healthy!"
                break
              fi
              if [ $i -eq 30 ]; then
                echo "❌ Container failed to become healthy"
                docker logs test-portfolio
                exit 1
              fi
              sleep 2
              i=$((i + 1))
            done
            
            # Test health endpoint
            docker exec test-portfolio wget -q -O- http://localhost:8000/health
            echo "✅ Health check passed"
            
            # Test homepage (should return content)
            HOMEPAGE_SIZE=$(docker exec test-portfolio wget -q -O- http://localhost:8000 | wc -c)
            if [ "$HOMEPAGE_SIZE" -gt 1000 ]; then
              echo "✅ Homepage test passed (${HOMEPAGE_SIZE} bytes)"
            else
              echo "❌ Homepage test failed (${HOMEPAGE_SIZE} bytes)"
              exit 1
            fi
            
            # Show container stats
            echo "📊 Container stats:"
            docker stats --no-stream test-portfolio
            
            # Cleanup
            docker stop test-portfolio
            docker rm test-portfolio
      
      - run:
          name: Save Docker image for deployment
          command: |
            echo "💾 Saving Docker image..."
            docker save portfolio-app:latest | gzip > portfolio-app.tar.gz
            ls -lh portfolio-app.tar.gz
      
      - persist_to_workspace:
          root: .
          paths:
            - portfolio-app.tar.gz

  # Deploy to production server
  deploy-production:
    executor: docker-executor
    steps:
      - checkout
      - attach_workspace:
          at: .
      - add_ssh_keys:
          fingerprints:
            - "SHA256:XUKEgesVvQ7EG6ERyVsv/PnQ21/qSbQNvQN1GR47N7c"
      
      - run:
          name: Deploy to OCI server
          command: |
            echo "🚀 Starting deployment to production server..."
            
            # Server details
            SERVER_HOST="129.80.244.212"
            SERVER_USER="ubuntu"
            
            # Transfer Docker image
            echo "📤 Transferring Docker image..."
            scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
                portfolio-app.tar.gz ${SERVER_USER}@${SERVER_HOST}:/tmp/
            
            echo "🏃 Executing deployment on server..."
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
                ${SERVER_USER}@${SERVER_HOST} "/opt/portfolio/deploy.sh"
            
            echo "✅ Deployment completed successfully!"
            echo ""
            echo "🌐 Portfolio URLs:"
            echo "  • Production: http://129.80.244.212"
            echo "  • Health check: http://129.80.244.212/health"
            echo ""
            echo "📊 Deployment summary:"
            echo "  • Image: portfolio-app:latest"
            echo "  • Container: portfolio-app"
            echo "  • Server: ${SERVER_HOST}"
            echo "  • Status: ✅ Live"

  # Health check job (can be run independently)
  health-check:
    executor: docker-executor
    steps:
      - run:
          name: Check production health
          command: |
            echo "🏥 Checking production server health..."
            
            SERVER_URL="http://129.80.244.212"
            
            # Check health endpoint
            if curl -f -s "${SERVER_URL}/health" -o /dev/null; then
              echo "✅ Health check passed"
              curl -s "${SERVER_URL}/health" | jq . 2>/dev/null || curl -s "${SERVER_URL}/health"
            else
              echo "❌ Health check failed"
              exit 1
            fi
            
            # Check homepage
            HOMEPAGE_SIZE=$(curl -s "${SERVER_URL}/" | wc -c)
            if [ "$HOMEPAGE_SIZE" -gt 1000 ]; then
              echo "✅ Homepage check passed (${HOMEPAGE_SIZE} bytes)"
            else
              echo "❌ Homepage check failed (${HOMEPAGE_SIZE} bytes)"
              exit 1
            fi
            
            echo "🎉 All health checks passed!"

# Workflows
workflows:
  # Main deployment workflow
  build-and-deploy:
    jobs:
      - build-and-test:
          filters:
            branches:
              ignore: /^dependabot\/.*/
      
      - deploy-production:
          requires:
            - build-and-test
          filters:
            branches:
              only:
                - master
                - main
          context: production-deploy
      
      - health-check:
          requires:
            - deploy-production
          filters:
            branches:
              only:
                - master
                - main

  # Scheduled health checks (every 6 hours)
  scheduled-health-check:
    triggers:
      - schedule:
          cron: "0 0,6,12,18 * * *"
          filters:
            branches:
              only: main
    jobs:
      - health-check

  # Weekly dependency and security check
  weekly-maintenance:
    triggers:
      - schedule:
          cron: "0 2 * * 1"  # Monday at 2 AM
          filters:
            branches:
              only: main
    jobs:
      - build-and-test